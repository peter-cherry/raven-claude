# === app code allowed to sync to Clean ===
+ /src/**
+ /app/**
+ /public/**
+ /docs/**
+ /compliance/**
# === root configs needed for builds ===
+ /package.json
+ /package-lock.json
+ /pnpm-lock.yaml
+ /yarn.lock
+ /tsconfig*.json
+ /next.config.*
+ /vite.config.*
+ /.eslintrc*
+ /.prettierrc*
# === block everything else (including .github) ===
- /*
